<?xml version="1.0" encoding="UTF-8" ?>
<XxgMapper namespace="com.dbc.meeting_system.dao.TestDao">

    <select id="test" resultType="com.dbc.meeting_system.entity.User" >
        SELECT
          *
        FROM
          t_user
        LIMIT 1
    </select>

    <delete id="deleteById">
        DELETE FROM t_user WHERE uId = #{uId}
    </delete>

    <insert id="insert" >
        INSERT INTO t_user() VALUES()

    </insert>
    <update>

    </update>

</XxgMapper>